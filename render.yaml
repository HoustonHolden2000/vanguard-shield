services:
  - type: web
    name: vanguard-shield
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
    disk:
      name: shield-data
      mountPath: /data
      sizeGB: 1
